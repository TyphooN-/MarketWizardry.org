---
- name: restart plausible
  community.docker.docker_compose_v2:
    project_src: "{{ plausible_deploy_dir }}"
    state: restarted

- name: restart apache
  systemd:
    name: apache2
    state: restarted
